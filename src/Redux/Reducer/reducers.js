const initialState = {
    users: [],
    user:{},
    loading: false,

};

const userReducer = (state=initialState)=>{
    switch(action.type){
        default: return  state
    }

}

export default userReducer;